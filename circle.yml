
machine:
  node:
    version: 4.3.1

dependencies:
  override:
    - gem install sass
    - npm install

test:
  override:
    - npm run lint
    - npm run test-old
    - npm run test-unit
    - npm run test-integration
