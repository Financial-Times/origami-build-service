
machine:
  node:
    version: 6

test:
  override:
    - make verify
    - make test
    - make deploy-vcl-then-test-ci

deployment:
  staging:
    branch: master
    commands:
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - make deploy-qa
